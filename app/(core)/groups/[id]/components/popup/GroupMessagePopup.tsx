import { Menu, MenuItem } from "@mui/material"

const GroupMessagePopup: React.FC = () => {
  return (
    <Menu
        id="message-menu"
        open>
          <MenuItem onClick={() => {}}>Delete</MenuItem>
    </Menu>
  )
}

export default GroupMessagePopup
