"use client"

// Use this link component if the children is not showing in the i18n navigation

// Re-export from the file that creates the `<Link />` component
export { Link as default } from "@/src/i18n/navigation"
